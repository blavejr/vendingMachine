// Import necessary dependencies and the Landing component
import React from 'react';
import { render, screen } from '@testing-library/react';
import Landing from './Landing';

// Test for rendering the Landing component
test('renders Landing component', () => {
  // Render the Landing component
//   render(<Landing />);
  
//   // Use the `screen.getByText` function to check for elements on the Landing page
//   const welcomeText = screen.getByText(/Welcome to Vending Machine/i);
//   const loginLink = screen.getByRole('link', { name: /Login/i });
//   const registerLink = screen.getByRole('link', { name: /Register/i });
  
//   // Assert that the elements are present
//   expect(welcomeText).toBeInTheDocument();
//   expect(loginLink).toBeInTheDocument();
//   expect(registerLink).toBeInTheDocument();
});
